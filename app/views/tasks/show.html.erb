<h1>タスク：<%= @task.name %>

<p><%= @task.description =%></p>

<ul>
  <li><%= link_to "Edit", edit_task_path(@task) %></li>
  <li><%= button_to "Delete", task_path(@task), method: :delete %></li>
</ul>
